[phases.setup]
# DON'T specify python here — let Nixpacks auto-detect Python
nixPkgs = ["ollama"] 

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "ollama serve & sleep 5 && streamlit run emne.py --server.port=8501 --server.address=0.0.0.0"
